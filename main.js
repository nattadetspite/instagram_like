console.log("test");

window.addEventListener("load", () => {
  const testButton = document.getElementById("test-button");
  testButton.addEventListener("click", () => {
    console.log("hello_test");
    // connect web with camera
    // capture
    // process image
    // result
    // display result image
  });
});
